namespace GhostPaste;

internal static class LocalSecrets
{
    // Copy this file to LocalSecrets.g.cs for local experimental builds.
    // LocalSecrets.g.cs is ignored by Git and must not be committed.
    public const string ApiKey = "";
}
